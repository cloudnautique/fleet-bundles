defaultNamespace: hello-world

targetCustomizations:
- name: ingress
  yaml:
    overlays:
    - ./overlays/ingress
  clusterSelector:
    matchLabels:
      ingress: "true"
- name: lb
  yaml:
    overlays:
    - ./overlays/service-lb
  clusterSelector:
    matchLabels:
      ingress: "false"

